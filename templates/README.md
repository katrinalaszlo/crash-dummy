# Report template

`report-template.html` is the crash report's design system. Fill the `{{SLOTS}}`,
duplicate the scenario card / findings row markup per item, embed screenshots as
data URIs, delete unused sections. Do not redesign the page: every crash-dummy
report should be recognizable at a glance.
